<header class="page-header">
  <h1>
    New User
    <br>
    <small>
      You may also login as a <%= link_to('guest', new_session_path) %>
    </small>
  </h1>
</header>

<%= render :partial => 'form', :locals => { :user => @user } %>

